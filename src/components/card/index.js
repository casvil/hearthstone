import React from 'react';
import PropTypes from 'prop-types';
import { Card } from 'components';

const Card = ({}) => {
  return (
    <div></div>
  );
}

Card.propTypes = {
  // : PropTypes.
};

export default Card;
