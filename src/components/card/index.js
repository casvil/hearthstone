import React, { Component } from 'react';
import PropTypes from 'prop-types';

class Card extends Component {
  render() {
    return (
      <div></div>
    );
  }
}

Card.propTypes = {
  // : PropTypes.
};

export default Card;
